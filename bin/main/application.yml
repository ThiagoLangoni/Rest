spring:
    datasource:
        url: jdbc:h2:~/fiapdb;DB_CLOSE_ON_EXIT=FALSE
        username: fiap
        password: fiap
    h2:
        console:
            enabled: true
            path: /db
    security:
        user:
            name: admin
            password: admin
            roles: ADMIN
jwt:
    secret: fiapsecretjwt
    expire: 1